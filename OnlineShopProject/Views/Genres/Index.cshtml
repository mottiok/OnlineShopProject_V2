@using System.Collections.Specialized
@model IEnumerable<OnlineShopProject.Models.GenreModel>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model)
{
    @*<li>
            <a href="@Url.Action("Filter", "Albums", new {genreId = item.Id})">
                @Html.DisplayFor(modelItem => item.Name)
            </a>
        </li>*@
    @Helpers.AddParamToUrl("genreId", item.Id.ToString(), item.Name, new UrlHelper(Request.RequestContext));
}
