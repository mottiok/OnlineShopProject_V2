@using System.Collections.Specialized
@model IEnumerable<OnlineShopProject.Models.GenreModel>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model)
{
    @Helpers.AddParamToUrl("genre", item.Id.ToString(), item.Name, new UrlHelper(Request.RequestContext));
}
