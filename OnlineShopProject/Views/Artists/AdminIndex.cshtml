@model IEnumerable<OnlineShopProject.Models.ArtistModel>

<section>
    <div class="container">
        <div class="row">
            <h2 class="title text-center admin-title">Edit Artists Items</h2>
            @using (Html.BeginForm("AdminIndex", "Artists", FormMethod.Get))
            {
                <div class="search_box">
                    <input type="text" placeholder="Search" name="SearchPattern" value="@ViewBag.SearchPattern" autofocus>
                </div>
                <input type="submit" value="Submit" class="hidden" />
            }
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</section>