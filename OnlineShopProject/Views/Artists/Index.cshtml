@using System.Collections.Specialized
@model IEnumerable<OnlineShopProject.Models.ArtistModel>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model)
{
    @Helpers.AddParamToUrl("artist", item.Id.ToString(), item.Name, new UrlHelper(Request.RequestContext));
}